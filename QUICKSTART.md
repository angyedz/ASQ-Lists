# 🚀 ASQ-Lists - БЫСТРЫЙ СТАРТ

## ДЛЯ ЛОКАЛЬНОГО ТЕСТИРОВАНИЯ (рекомендуется)

### Шаг 1: Запустить Backend
```powershell
python backend_server.py
```

### Шаг 2: Запустить CORS Прокси
```powershell
python cors_proxy.py
```

### Шаг 3: Открыть приложение
```
https://angyedz.github.io/ASQ-Lists/
```

✅ **Всё работает!** Никаких туннелей не нужно!

---

## ДЛЯ ИНТЕРНЕТА (если нужна интернет-версия)

### Вариант 1: Cloudflare Tunnel (РЕКОМЕНДУЕТСЯ - бесплатно, надёжно)

```powershell
# Установить cloudflared
choco install cloudflare-warp

# Запустить туннель
cloudflared tunnel --url http://localhost:9000

# Скопировать URL из вывода (например: https://xxx.trycloudflare.com)
```

Потом обновить `js/api.js`:
```javascript
export const AUTH_URL = 'https://xxx.trycloudflare.com';
```

### Вариант 2: ngrok

```powershell
ngrok http 9000
```

**ВАЖНО:** Откройи ngrok URL в браузере один раз перед использованием!

### Вариант 3: localtunnel

```powershell
lt --port 9000 --subdomain asq-lists
```

---

## Тестовые аккаунты

- Пользователь: `kazah`
- Пароль: `88888888`

или создай новый через Register

---

## Архитектура

```
GitHub Pages Frontend     CORS Прокси         Backend
https://angyedz.github.io/  →  localhost:8001  →  localhost:9000
    ASQ-Lists/                (cors_proxy.py)    (backend_server.py)
```

---

## Устранение проблем

| Проблема | Решение |
|----------|---------|
| "Cannot connect to server" | Убедись что `python backend_server.py` запущен |
| CORS ошибки | Убедись что `python cors_proxy.py` запущен |
| API возвращает HTML | Для ngrok: откройи URL в браузере один раз |
| Data файлы 404 | Дождись 1-2 минут GitHub Pages rebuild |

---

## Контакты

Если что-то не работает - проверь консоль F12 в браузере (там ошибки детальнее)

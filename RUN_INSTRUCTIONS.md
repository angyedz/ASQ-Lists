# 🚀 ASQ-Lists - Инструкции по запуску

## Требования

1. Python 3.x
2. Node.js (для ngrok и localtunnel)

## Три шага для запуска

### 1️⃣ **Запустить Backend сервер**

```powershell
python backend_server.py
```

Слушает на: `http://localhost:9000`

### 2️⃣ **Запустить туннель (выбери один из вариантов)**

#### Вариант A: ngrok (рекомендуется)
```powershell
ngrok http 9000
```

Скопируй URL из вывода (например: `https://xyz.ngrok-free.dev`)

Затем обнови `js/api.js`:
```javascript
export const AUTH_URL = 'https://xyz.ngrok-free.dev';
```

**ВАЖНО:** Первый раз откройнj ngrok URL в браузере для одобрения!

#### Вариант B: localtunnel
```powershell
lt --port 9000 --subdomain asq-lists
```

URL: `https://asq-lists.loca.lt`

Обнови `js/api.js`:
```javascript
export const AUTH_URL = 'https://asq-lists.loca.lt';
```

### 3️⃣ **Обновить API URL в коде**

После получения туннель URL, обнови файл `js/api.js` (строка 6):

```javascript
export const AUTH_URL = 'ТВ​ОЙ_ТУННЕЛЬ_URL';
```

Затем:
```powershell
git add js/api.js
git commit -m "Update API URL"
git push
```

Дождись 1-2 минуты для GitHub Pages rebuild.

## Тестирование

1. **Открой** https://angyedz.github.io/ASQ-Lists/ 
2. **Проверь F12 Console** - не должно быть ошибок
3. **Попробуй**:
   - ✅ Загрузить список уровней (List)
   - ✅ Посмотреть лидерборд (Leaderboard)
   - ✅ Зарегистрироваться (Auth)
   - ✅ Залогиниться (Login)

## Аккаунты для тестирования

- Юзер: `kazah`
- Пароль: `88888888`

## Решение проблем

### ngrok показывает браузерную страницу
- Откройте URL в браузере один раз
- Или добавьте User-Agent header в запросы

### localtunnel быстро закрывается
- Проверьте что терминал остаётся открытым
- Используйте `lt --port 9000` без `--subdomain`

### CORS ошибки
- Убедитесь что AUTH_URL правильный
- Проверьте что `credentials: 'omit'` в fetch запросах


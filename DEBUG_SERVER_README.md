# 🖥️  Debug Server - Локальный хост для тестирования

## Назначение

Простой локальный сервер для дебугинга на одной машине:
- ✅ Система аккаунтов (вход/регистрация)
- ✅ Лидерборд
- ✅ Сохранение данных в файлы
- ✅ БЕЗ CORS проблем (всё на одном localhost)

## Как использовать

### 1️⃣ Запустить сервер

```bash
cd "my site"
python debug_server.py
```

Вывод:
```
======================================================================
🖥️  ASQ LIST - DEBUG SERVER
======================================================================

✅ Сервер на http://localhost:8000
📁 Файлы из: D:\projects\site and lib\my site

📊 Данные пользователей:
  • accounts.txt (пользователи)
  • leaderboard.json (лидерборд)

🔧 Эндпоинты:
  • POST /api/auth (login, reg)
  • POST /api/leaderboard (get, update)

⌨️  Press Ctrl+C to stop
======================================================================
```

### 2️⃣ Проверить в браузере

Откройте: **http://localhost:8000**

## 📝 Как работает

### Вход/Регистрация

```bash
curl -X POST http://localhost:8000/api/auth \
  -H "Content-Type: application/json" \
  -d '{
    "mode": "reg",
    "user": "testuser",
    "pwd": "password123"
  }'
```

Ответ:
```json
{
  "status": "success",
  "user": "testuser"
}
```

### Лидерборд

```bash
# Получить лидерборд
curl -X POST http://localhost:8000/api/leaderboard \
  -H "Content-Type: application/json" \
  -d '{"action": "get"}'

# Обновить запись
curl -X POST http://localhost:8000/api/leaderboard \
  -H "Content-Type: application/json" \
  -d '{
    "action": "update",
    "user": "testuser",
    "level": "Level 1",
    "score": 100
  }'
```

## 📁 Файлы данных

### `accounts.txt`
Хранит пользователей в формате:
```
username:password
testuser:password123
admin:admin12345
```

### `leaderboard.json`
Хранит лидерборд:
```json
[
  {
    "user": "testuser",
    "level": "Level 1",
    "score": 100
  }
]
```

## 🔧 Конфигурация

API URL уже настроен в `js/api.js`:

```javascript
// 🖥️  ДЛЯ ДЕБУГИНГА:
export const API_URL = 'http://localhost:8000';
```

## ✅ Валидация

Сервер проверяет:

```
✓ Username: 3-20 символов, только буквы/цифры/_
✓ Password: минимум 8 символов
✓ Mode: только "login" или "reg"
✓ JSON: валидный JSON формат
```

## 🐛 Дебугинг

Смотрите консоль сервера:

```
[127.0.0.1] POST /api/auth HTTP/1.1
✓ testuser registered

[127.0.0.1] POST /api/auth HTTP/1.1
✓ testuser logged in

[127.0.0.1] POST /api/leaderboard HTTP/1.1
✓ testuser - Level 1: 100 points
```

## 📱 DevTools

Откройте DevTools (F12) → Network → видите все запросы

## 🎯 Тестирование

1. Откройте http://localhost:8000
2. Кликните "Account" → "Register here"
3. Введите username, password (8+ символов)
4. Введите капчу (простая математика)
5. Кликните "Register"
6. Должна открыться страница профиля

Пользователь сохранится в `accounts.txt`

## ⚙️ Что здесь упрощено

- ❌ Нет защиты от атак (это локальный хост)
- ❌ Нет хеширования паролей (хранятся как есть)
- ❌ Нет JWT токенов (просто username)
- ✅ Зато всё просто и работает локально без проблем

## 🚀 Как перейти на production

1. Используйте `simple_backend.py` вместо `debug_server.py`
2. Настройте cloudflared туннель
3. Скопируйте туннель-URL в `js/api.js`
4. Добавьте правильное хеширование паролей
5. Добавьте защиту (смотрите старые файлы SECURITY.md если нужна)

## ❓ Часто задаваемые вопросы

**Q: Почему я вижу ошибку "Invalid credentials"?**
A: Проверьте что вы вводите точный username/password

**Q: Где хранятся пароли?**
A: В файле `accounts.txt` рядом с сервером (ВНИМАНИЕ: открытые пароли!)

**Q: Можно ли использовать для production?**
A: НЕТ! Только для локального тестирования!

**Q: Как очистить данные?**
A: Удалите `accounts.txt` и `leaderboard.json`

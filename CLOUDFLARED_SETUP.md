# ⚡ CLOUDFLARED + GITHUB PAGES - БЫСТРЫЙ ГАЙД

## 🎯 Архитектура

```
Ваш телефон           GitHub Pages            Cloudflared Туннель
(бекенд)              (фронтенд)              (шифрование)
    │                     │                           │
    │◄─────────────────────┼─────────────────────────►│
    │  HTTPS через туннель │ (безопасно)              │
    │                     │                           │
    └─────────────────────┴───────────────────────────┘
```

---

## 1️⃣ Установка (5 минут)

### На компьютере / телефоне (где будет бекенд):

```bash
# 1. Установить Python зависимости
pip install flask flask-cors

# 2. Скачать Cloudflared
# https://developers.cloudflare.com/cloudflare-one/connections/connect-applications/install-and-setup/installation/

# Или через pip:
pip install cloudflared
```

### На GitHub:

```bash
# 1. Создать новый репозиторий "username.github.io"
# 2. Скопировать туда файлы из "my site" (кроме .py файлов)
# 3. Включить GitHub Pages в Settings
```

---

## 2️⃣ Запуск бекенда (3 команды)

### Терминал 1 - Запустить бекенд:
```bash
cd "my site"
python simple_backend.py
```

Вывод:
```
============================================================
🚀 ASQ LIST - ПРОСТОЙ БЕКЕНД
============================================================

✅ Локальный сервер на http://localhost:8000
...
```

### Терминал 2 - Запустить туннель Cloudflared:
```bash
cloudflared tunnel --url http://localhost:8000
```

Вывод типа:
```
Your quick tunnel has been created! Visit it at (it may take some time to be reachable):
https://xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx.trycloudflare.com
```

---

## 3️⃣ Обновить фронтенд

Откройте `js/api.js` и замените:

```javascript
// ⚠️ ИЗМЕНИТЕ ЭТО НА ВАШ CLOUDFLARED URL:
export const API_URL = 'https://xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx.trycloudflare.com';
```

---

## 4️⃣ Загрузить на GitHub Pages

```bash
# Скопируйте все файлы (кроме .py и .md) в GitHub Pages репо

# Или через git:
git add .
git commit -m "Update API URL"
git push origin main
```

---

## ✅ Готово!

Откройте https://username.github.io и всё должно работать! 🎉

---

## 🔄 Как это работает

```
1. Пользователь кликает на сайт в GitHub Pages
   ↓
2. Фронтенд отправляет запрос на https://xxx.trycloudflare.com
   ↓
3. Cloudflared туннель перенаправляет на http://localhost:8000
   ↓
4. Бекенд на телефоне обрабатывает запрос
   ↓
5. Ответ возвращается через туннель обратно
   ↓
6. Фронтенд показывает результат
```

---

## ⚠️ ВАЖНО

- **Туннель временный** - каждый раз при перезапуске меняется URL
- **Решение:** Используйте постоянный туннель через Cloudflare аккаунт
- **Лучше:** Зарегистрируйте свой домен в Cloudflare и используйте его

---

## 🚀 Для постоянного туннеля

```bash
# 1. Зарегистрируйтесь в Cloudflare
# 2. Добавьте свой домен
# 3. Используйте:
cloudflared tunnel create my-asq-list
cloudflared tunnel route dns my-asq-list api.your-domain.com
cloudflared tunnel run my-asq-list
```

---

## 📝 Что на сервере (simple_backend.py)

- ✅ Вход/регистрация пользователей
- ✅ Лидерборд
- ✅ Health check
- ✅ CORS включён (работает с любым фронтендом)
- ❌ Нет защиты (т.к. это просто для демо)

Для production добавьте защиту из `SECURITY.md`

---

## 💡 Советы

**Не забывайте:**
1. Бекенд должен быть запущен всегда (иначе ошибка)
2. Туннель должен быть запущен всегда
3. URL туннеля меняется при перезапуске (если не постоянный)

**Для телефона:**
1. Установите Termux
2. Установите Python через Termux
3. Запустите простой_backend.py в Termux
4. Туннель должен работать постоянно

---

## 📊 Минимальный набор файлов для GitHub Pages

```
username.github.io/
├── index.html           ← Главная страница
├── css/                 ← Все CSS файлы
├── js/
│   ├── main.js
│   ├── api.js          ← ⚠️ С вашим cloudflared URL
│   ├── routes.js
│   ├── store.js
│   └── pages/          ← Все компоненты
├── data/               ← JSON данные
└── assets/             ← Иконки и картинки

❌ НЕ загружайте:
- *.py файлы
- SECURITY.md, SETUP.md и т.д.
```

---

## 🎉 Готово!

Теперь у вас есть:
- 📱 Бекенд на телефоне (или компьютере)
- 🌐 Фронтенд на GitHub Pages
- 🔐 Защищённая связь через Cloudflared туннель

**Начните:** `python simple_backend.py`

# ASQ Lists - Website Template & Security Setup

Это точная копия шаблона GDListTemplate, адаптированная под ASQ Lists с встроенной защитой.

## ⚠️ ВАЖНО: Безопасность

Если вы планируете использовать отдельный бекенд-хост, **обязательно** прочитайте [`SECURITY.md`](./SECURITY.md) для информации о защите от атак.

## Структура проекта

```
my site/
├── .editorconfig          # Конфигурация редактора
├── .gitignore             # Git ignore файл
├── index.html             # Главная страница (Vue.js приложение)
├── README.md              # Документация
│
├── assets/                # Папка для логотипов и иконок
│
├── css/                   # Стили
│   ├── main.css
│   ├── reset.css
│   ├── typography.css
│   ├── components/
│   │   ├── btn.css
│   │   ├── nav.css
│   │   └── tabs.css
│   └── pages/
│       ├── list.css
│       ├── leaderboard.css
│       └── roulette.css
│
├── js/                    # JavaScript файлы
│   ├── main.js            # Точка входа Vue приложения
│   ├── routes.js          # Маршруты приложения
│   ├── store.js           # Общее состояние (тёмный режим)
│   ├── util.js            # Утилиты (YouTube embed, shuffle и т.д.)
│   ├── score.js           # Система подсчета очков
│   ├── content.js         # Загрузка данных списков и лидерборда
│   ├── components/
│   │   ├── Spinner.js     # Компонент загрузки
│   │   ├── Btn.js         # Кнопка
│   │   └── List/
│   │       └── LevelAuthors.js  # Информация об авторах уровня
│   └── pages/
│       ├── List.js        # Страница со списком уровней
│       ├── Leaderboard.js # Страница лидерборда
│       └── Roulette.js    # Страница рулетки
│
└── data/                  # Данные списка
    ├── _list.json         # Список имён всех уровней
    ├── _editors.json      # Информация об редакторах списка
    ├── SampleLevel1.json  # Пример уровня 1
    ├── SAMPLELEVEL2.json  # Пример уровня 2
    └── Sample_Level_3.json # Пример уровня 3
```

## Основные функции

✅ **Список уровней** - Отображение всех уровней с информацией о создателях, верификаторах и рекордах
✅ **Лидерборд** - Рейтинг игроков по набранным очкам
✅ **Рулетка** - Случайный выбор уровней для испытания
✅ **Тёмный режим** - Переключение между светлой и тёмной темой
✅ **Responsive дизайн** - Адаптивность под разные размеры экранов
✅ **🔒 Безопасность** - Встроенная защита от основных типов атак

## Локальная разработка

```bash
cd "my site"
python server.py
```
Откроется на `http://localhost:8000`

## Production с отдельным бекенд-хостом

### 1. Настройка фронтенда
Обновьте `js/api.js`:
```javascript
export const API_URL = 'https://api.example.com';
```

### 2. Настройка бекенда на отдельном сервере
```bash
# Установите зависимости
pip install -r requirements.txt

# Запустите с Gunicorn (production)
gunicorn -w 4 -b 0.0.0.0:8000 wsgi_secure_server:app
```

### 3. Используйте Nginx как reverse proxy
Смотрите [`SECURITY.md`](./SECURITY.md) для примера конфигурации Nginx.

## Настройка

1. Замени название списка в `index.html` (сейчас "ASQ List")
2. Добавь логотип `list_icon.png` в папку `assets/`
3. Добавь ссылку на Discord сервер в `index.html`
4. Добавь ссылку на форму подачи рекордов в `index.html`
5. Замени примеры уровней в папке `data/` на реальные уровни
6. Обнови информацию об редакторах в `_editors.json`

## 🔒 Безопасность

Проект включает встроенную защиту от:
- ✅ DDoS атак (rate limiting)
- ✅ Brute-force атак (ограничение попыток входа)
- ✅ SQL injection (валидация входных данных)
- ✅ XSS атак (sanitization + security headers)
- ✅ CSRF атак (CORS protection)
- ✅ Directory traversal
- ✅ Buffer overflow (size limits)

**Читай [`SECURITY.md`](./SECURITY.md) для полной информации!**

## Технологии

- Vue.js 3
- Vue Router 4
- Vanilla CSS (+ CSS Variables для тем)
- Lexend Deca шрифт
- Python (Flask/Gunicorn для бекенда)

## Troubleshooting

### Проблема: CORS errors в DevTools
**Решение:** Обновите `ALLOWED_ORIGINS` в `secure_server.py` или `wsgi_secure_server.py`

### Проблема: Rate limiting (429 ошибка)
**Решение:** Это нормально при большом количестве запросов. Подождите 1 минуту и попробуйте снова.

## Лицензия и кредиты

Оригинальный шаблон: [TheShittyList](https://tsl.pages.dev/)
Адаптирован для ASQ Lists с встроенной безопасностью
